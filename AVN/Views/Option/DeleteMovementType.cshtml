@model MovementType
<div class="container">
    <h1>Подтверждение удаления</h1>
    <p>Вы уверены, что хотите удалить этот элемент?</p>
    <div class="card">
        <div class="card-body">
            <p class="card-text">@Model.Name</p>
            <form method="post" action="/Option/DeleteMovementTypeConfirmed/@Model.Id">
                <button type="submit" class="btn btn-danger">Удалить</button>
                <a href="/Option/Index" class="btn btn-secondary">Отмена</a>
            </form>
        </div>
    </div>
</div>
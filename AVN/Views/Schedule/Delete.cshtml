@model AVN.Model.Entities.Schedule

<div class="container">
    <h1>Подтверждение удаления</h1>
    <p>Вы уверены, что хотите удалить этот элемент?</p>
    <div class="card">
        <div class="card-body">
            <p class="card-text">@Model.Subject.Title</p>
            <form method="post" action="/Schedule/DeleteConfirmed/@Model.Id">
                <button type="submit" class="btn btn-danger">Удалить</button>
                <a href="/Schedule/Index" class="btn btn-secondary">Отмена</a>
            </form>
        </div>
    </div>
</div>